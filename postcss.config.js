// postcss.config.js
module.exports = {
    plugins: [
      require('tailwindcss'), // Includes Tailwind CSS plugin
      require('autoprefixer'), // Automatically adds vendor prefixes to CSS rules
    ],
  }
  